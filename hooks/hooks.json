{
  "description": "statusline-injector hooks. SessionStart: идемпотентно оборачивает statusLine.command в settings.json (Claude — захват лимитов 5h/7d из stdin harness в state-файл; на Codex no-op). UserPromptSubmit: рендерит и инжектит одну минимальную строку статуса в контекст хода (оба рантайма).",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT:-${PLUGIN_ROOT}}/scripts/statusline-session-start.sh\"",
            "timeout": 10
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT:-${PLUGIN_ROOT}}/scripts/statusline-inject.sh\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
