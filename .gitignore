# macOS
.DS_Store
._*

# Editor swap / backup files
*~
*.swp
*.bak
*.bak.*

# Runtime state (written by statusline-state-writer.sh at $HOME, not in repo;
# ignore accidental copies / test runs landing next to the scripts)
state.json
config.env

# Local test artifacts
/tmp/

# Claude Code working files — keep out of this public repo
CLAUDE.md
.claude/

# Agent / runtime session artifacts — local only, never in the public repo
AGENTS.md
.codex/
.iapeer/
